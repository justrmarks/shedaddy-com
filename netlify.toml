[build]
  publish = "public"
  command = "npm run build"
  functions = "lambda"

[build.environment]
  YARN_VERSION = "1.9.4"
  YARN_FLAGS = "--no-ignore-optional"
  GO_IMPORT_PATH = "github.com/justrmarks/shedaddy-com"
